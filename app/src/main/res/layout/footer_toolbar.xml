<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/bottom_toolbar"
    android:orientation="horizontal"
    android:padding="@dimen/xsmall"
    android:background="@color/colorSecondaryBackground"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_alignParentBottom="true">

    <TextView
        android:id="@+id/bottom_toolbar_title"
        android:text="@string/bottom_toolbar_title"
        android:fontFamily="sans-serif-condensed"
        android:textColor="@color/colorPrimaryText"
        android:textSize="18sp"
        android:layout_marginLeft="@dimen/normal"
        android:layout_marginStart="@dimen/normal"
        android:layout_centerVertical="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>


    <ImageButton
        android:id="@+id/button_audio"
        android:contentDescription="@string/image_button_description"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:background="@android:color/transparent"
        android:layout_width="@dimen/button_size"
        android:layout_height="@dimen/button_size"/>

    <ImageButton
        android:id="@+id/button_photo"
        android:contentDescription="@string/image_button_description"
        android:layout_toLeftOf="@id/button_audio"
        android:layout_toStartOf="@id/button_audio"
        android:layout_marginEnd="@dimen/normal"
        android:layout_marginRight="@dimen/normal"
        android:background="@android:color/transparent"
        android:layout_width="@dimen/button_size"
        android:layout_height="@dimen/button_size"/>

    <ImageButton
        android:id="@+id/button_text"
        android:layout_toLeftOf="@id/button_photo"
        android:layout_toStartOf="@id/button_photo"
        android:contentDescription="@string/image_button_description"
        android:layout_marginEnd="@dimen/normal"
        android:layout_marginRight="@dimen/normal"
        android:background="@android:color/transparent"
        android:layout_width="@dimen/button_size"
        android:layout_height="@dimen/button_size"/>


</RelativeLayout>